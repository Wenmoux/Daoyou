# map/
> L2 | 父级: ../CLAUDE.md

地图场景把静态世界节点投影成可操作的详情抽屉。节点的 fishing_config 只决定可进入的水域，具体会话和权限仍由垂钓服务端校验。

成员清单

route.tsx: 地图画布、节点选择和地图动作调用。
mapActions.ts: 按地图意图生成历练、坊市与垂钓入口。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
